{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ServiceNowConfig</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Businessservice</th>
                <td>{{ entity.businessService }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ entity.category }}</td>
            </tr>
            <tr>
                <th>Subcategory</th>
                <td>{{ entity.subcategory }}</td>
            </tr>
            <tr>
                <th>Configurationitem</th>
                <td>{{ entity.configurationItem }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('servicenowconfig') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('servicenowconfig_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
